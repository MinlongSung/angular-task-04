import { RotateImageDirective } from './rotate-image.directive';

describe('RotateImageDirective', () => {
  it('should create an instance', () => {
    // const directive = new RotateImageDirective();
    // expect(directive).toBeTruthy();
  });
});
